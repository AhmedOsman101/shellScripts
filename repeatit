#!/usr/bin/env bash

# shellcheck disable=SC1091
source logs.sh

cmd="$1"

# until $cmd; 
# do 
#   $cmd
# done

# Infinite loop: repeat the command until it exits successfully
while true; do
    if [ "$cmd" ]; then
        break
    fi

    
done
