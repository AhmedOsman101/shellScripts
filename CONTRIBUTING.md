# Contributing

We welcome contributions! If you have scripts you'd like to share or improvements for existing ones:

1. Fork the repository.
2. Generate your script with `mkscript` (bash) or `mkpython` (Python) so it follows the repo's signature, dependency, and argument-parsing conventions. Bare scripts that don't follow these patterns won't be accepted.
3. Submit a pull request.
