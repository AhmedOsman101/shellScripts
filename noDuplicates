#!/usr/bin/env bash

file="$1"

if [[ -f $file ]]; then
  tmpfile="$(mktemp)"
  trap 'rm -f $tmpfile' EXIT
  
  awk '!x[$0]++' "$file" >"$tmpfile"

  cat "$tmpfile" >"$file"

  log-info "Duplicates removed!"
else
  log-error "$file was not found!"
fi
